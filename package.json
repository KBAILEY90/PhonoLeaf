{
  "name": "phonoleaf",
  "private": true,
  "version": "1.0.0",
  "description": "PhonoLeaf native app shell (Capacitor). The web app itself is index.html — no build step; scripts/stage-www.js just copies the web files into www/ for the native wrapper.",
  "scripts": {
    "stage": "node scripts/stage-www.js",
    "sync": "node scripts/stage-www.js && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
